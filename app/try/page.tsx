// Placeholder — /try is rebuilt in Batch 2 as a client wrapper around <Board />
// backed by MSW mocks. See docs/01_PLAN_F4_INTERACTIVE_DEMO_PLAN.md.
export default function TryPage() {
  return null;
}
