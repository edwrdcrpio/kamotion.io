import { DemoGenerateForm } from "@/components/demo/demo-generate-form";

export const metadata = { title: "Generate Tasks" };

export default function TryGeneratePage() {
  return <DemoGenerateForm />;
}
