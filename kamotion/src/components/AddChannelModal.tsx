import React, { useState } from 'react'; import { authenticateGmail } from '../api/gmail'; // Import authenticateGmail import { addChannel as addSlackChannel } from '../api/slack'; // Import addChannel for Slack interface AddChannelModalProps { isOpen: boolean; onClose: () => void; onAddChannel: (channel: any) => void; // Add onAddChannel prop } const AddChannelModal: React.FC<AddChannelModalProps> = ({ isOpen, onClose, onAddChannel }) => { const [selectedChannelType, setSelectedChannelType] = useState(''); const [slackChannelId, setSlackChannelId] = useState(''); // State for Slack channel ID if (!isOpen) { return null; } const channelTypes = [Gmail, Slack, AI Chatbot, Notes]; const handleChannelTypeChange = (event: React.ChangeEvent<HTMLSelectElement>) => { setSelectedChannelType(event.target.value); }; const handleSlackChannelIdChange = (event: React.ChangeEvent<HTMLInputElement>) => { setSlackChannelId(event.target.value); }; const handleAddChannel = async () => { if (selectedChannelType === Slack) { console.log(Adding Slack channel with ID:, slackChannelId); const newChannel = await addSlackChannel(slackChannelId); onAddChannel(newChannel); // Update channel list with the new channel onClose(); // Close modal after adding } else if (selectedChannelType === Gmail) { // Gmail is handled by the connect button separately onClose(); } else if (selectedChannelType === AI Chatbot) { // For now, just add a default AI Chatbot channel console.log(Adding AI Chatbot channel); onAddChannel({ type: AI Chatbot, name: New Chatbot, category: AI Chatbot, icon: ai }); // Update channel list onClose(); } else { console.log(Adding channel of type:, selectedChannelType); onClose(); } }; let channelInputs; switch (selectedChannelType) { case Gmail: channelInputs = ( <button className=bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded onClick={() => authenticateGmail()} > Connect Gmail </button> ); break; case Slack: channelInputs = ( <div> <label htmlFor=slackChannelId className=block text-gray-700 text-sm font-bold mb-2>Slack Channel ID:</label> <input type=text id=slackChannelId className=shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline value={slackChannelId} onChange={handleSlackChannelIdChange} /> </div> ); break; case AI Chatbot: channelInputs = <p>No input needed for AI Chatbot</p>; break; case Notes: channelInputs = <p>Notes Placeholder</p>; // Replace with actual Notes input break; default: channelInputs = null; } return ( <div className=fixed inset-0 bg-gray-500 bg-opacity-75 flex items-center justify-center> <div className=bg-white rounded-lg p-8 w-96> <h2 className=text-xl font-bold mb-4>Add Channel</h2> {/* Channel type selection */} <div className=mb-4> <label htmlFor=channelType className=block text-gray-700 text-sm font-bold mb-2>Channel Type:</label> <select id=channelType className=shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline value={selectedChannelType} onChange={handleChannelTypeChange} > <option value=>Select Channel Type</option> {channelTypes.map((type) => ( <option key={type} value={type}>{type}</option> ))} </select> </div> {/* Channel information inputs */} <div className=mb-4> {channelInputs} </div> <div className=flex justify-end> <button onClick={onClose} className=bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded mr-2>Cancel</button> <button onClick={handleAddChannel} className=bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded>Add</button> </div> </div> </div> ); }; export default AddChannelModal;
